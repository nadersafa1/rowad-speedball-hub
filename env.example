# Database Configuration
DATABASE_URL=postgresql://postgres:password@localhost:5432/speedball_hub

# Session Configuration
SESSION_SECRET=your-secret-key-change-in-production

# Server Configuration
PORT=8000
NODE_ENV=development

# Production Database (for production deployment)
DB_PASSWORD=secure_password_change_me

# Frontend Configuration (if needed)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Admin Credentials (configured in code, change for production)
# Default: admin@rowad.com / Test@1234

